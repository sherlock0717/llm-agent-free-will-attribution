# 公开页面重构内部审查记录（2026-07-28）

## 审查范围

- 项目根页面
- 研究A独立页面
- 研究B独立页面及方法文档
- Pages组装与部署工作流
- 公开JSON契约
- 研究A分析V2

本文件属于内部维护记录。公开页面采用`docs/PUBLIC_PRESENTATION_GUIDE.md`规定的正向研究叙事。

## 一、公开页面结构

### 已处理

1. 根页面从“项目总览＋研究A长报告”收缩为研究计划总览。
2. 新增研究A规范路径：`/identity-process-attribution-baseline/`。
3. 保留研究B规范路径：`/machine-decision-process-attribution/`。
4. 保留研究B旧路径兼容跳转：`/pa-wu-r1-pilot/`。
5. 根页面、研究A和研究B均提供返回与交叉入口。
6. 本地预览统一使用组装后的`_site/`。

### 页面职责

- 根页面：项目问题、研究计划、A/B入口、证据组成、共享方法、扩展路线、数据入口。
- 研究A：设计、场景、测量、数据、分析、结果与复现。
- 研究B：机器主体构念、D/U设计、材料、评分、P1—P6、流程演示与正式运行结构。

## 二、公开表达

### 已处理

1. 建立统一表达规范与数据类型字典。
2. 研究A公开名称统一为“DeepSeek API模型模拟问卷响应”。
3. 研究B demo统一为“流程演示数据”。
4. PA5与PA8统一解释为感知能动性的两个重叠评分版本。
5. 研究B六条件统一解释为六个预先指定的D/U组合。
6. 分析计划中的`pre-registered`改为`specified in advance`。
7. MSI保留完整复合分数，`wu_ms3`另作探索性单项报告。

## 三、研究A方法

### V2结构

核心单元：

```text
scenario_id × identity_label × process_condition
```

预期单元数：

```text
8 × 2 × 6 = 96
```

同一单元内的模型生成重复用于计算均值、标准差和记录数。主要条件比较在同一场景与身份内计算。

### V2公开字段

- 平均差异
- 中位数差异
- 正向/负向/零差异单元数
- 方向一致率
- 场景均值范围
- 留一场景均值范围

### 历史资产处理

- `outputs/`保持只读。
- V2默认写入`artifacts/research_a_v2/`。
- 既有OLS、Welch t检验与Bootstrap路径结果保留为历史探索性分析。

## 四、页面运行韧性

### 根页面

- 只依赖`showcase_story.json`补充动态事实。
- JSON异常时使用HTML内置说明，A/B入口继续可用。

### 研究A

数据分成三组独立加载：

1. 场景与结果摘要；
2. 构念与题项内部一致性；
3. 条件均值、身份效应与预设比较。

各组采用独立状态与重试入口，一个数据文件异常不会清空整页。

### 研究B

沿用现有静态研究内容＋动态流程演示数据的降级结构。

## 五、JSON与发布门禁

### 已处理

1. 新增浏览器兼容的严格JSON检查器。
2. 新增共享JSON安全序列化工具。
3. Pages部署前执行：
   - 站点生成数据检查；
   - 研究A V2构建；
   - 严格JSON检查；
   - `tests/site`；
   - Ruff；
   - Pages组装；
   - 组装后JSON检查。
4. Pages组装器检查根页面、A/B页面、根JSON、根图表、B JSON与B五图。

## 六、文件边界

本轮没有修改：

- 研究A历史CSV与图表；
- 研究B 96条材料；
- 研究B题项；
- 研究B模型ID；
- 研究B P1—P6；
- 研究B正式模型公式；
- 真实模型配置；
- 原始响应。

## 七、验证状态

已建立自动测试与发布工作流。当前分支尚未创建PR，因此GitHub连接器无法读取PR触发的CI结果。合并前需要完成：

1. 分支CI或PR CI；
2. 根页面、研究A、研究B的1440/768/390px浏览器检查；
3. 三个规范路径与一个兼容路径的本地HTTP检查；
4. 研究A V2输出数量与字段复核；
5. Pages组装产物资源检查。

## 八、后续批次

1. 将共享`public_json`写出函数接入所有站点生成器；
2. 将公开数据文件中的旧术语逐步迁移到统一来源字典；
3. 为研究A页面接入V2公开摘要与场景级可视化；
4. 对研究B页面进行最后一次PA5/PA8与状态文案同步；
5. 在CI通过后创建PR并进行线上Pages验收。
